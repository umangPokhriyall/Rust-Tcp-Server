[package]
name = "core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Phase 0 allowlist for `core`: socket2 only (used by `bind_listener` for
# SO_REUSEPORT / SO_REUSEADDR). The "all" feature exposes `set_reuse_port`.
socket2 = { version = "0.6", features = ["all"] }
